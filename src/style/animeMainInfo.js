import Styled from 'styled-components';

/**
 * This return a `div` component element
 *
 * @export const @type {Component}
 *
 * @return {Component} Returns a new component with the style applied
 */
export const MainInfoContainer = Styled.div`
  border: solid 1px #edeeee;
  max-width: 300px;
  display: inline-block;
`;

/**
 * This return a `div` component element
 *
 * @export const @type {Component}
 *
 * @return {Component} Returns a new component with the style applied
 */
export const PosterContainer = Styled.div`
  padding: 15px;
`;

/**
 * This return a `img` component element
 *
 * @export const @type {Component}
 *
 * @return {Component} Returns a new component with the style applied
 */
export const PosterImage = Styled.img`
  max-width: 220px;
`;

/**
 * This return a `div` component element
 *
 * @export const @type {Component}
 *
 * @return {Component} Returns a new component with the style applied
 */
export const AnimeDetails = Styled.div`
  background-color: #f5f8f9;
`;

/**
 * This return a `div` component element
 *
 * @export const @type {Component}
 *
 * @return {Component} Returns a new component with the style applied
 */
export const Row = Styled.div`
  padding: 10px 15PX;
  border-bottom: 1px solid #eee;
`;
/**
 * This return a `div` component element
 *
 * @export const @type {Component}
 *
 * @return {Component} Returns a new component with the style applied
 */
export const TitleDetail = Styled.div`
  width: 40%;
  display: inline-block;
  color: #0591da;
  text-transform: capitalize;
  font-weight: bold;
`;

/**
 * This return a `span` component element
 *
 * @export const @type {Component}
 *
 * @return {Component} Returns a new component with the style applied
 */
export const Detail = Styled.span``;
